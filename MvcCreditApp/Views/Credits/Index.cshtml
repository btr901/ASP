@model IEnumerable<MvcCreditApp.Models.Credit>

@{
    ViewData["Title"] = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h1>Главная</h1>

<p>
    <a asp-action="Create">Создать новый</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th bgcolor="c3c3c3">
                @Html.DisplayNameFor(model => model.Head)
            </th>
            <th bgcolor="c3c3c3">
                @Html.DisplayNameFor(model => model.Period)
            </th bgcolor="c3c3c3">
            <th bgcolor="c3c3c3">
                @Html.DisplayNameFor(model => model.Sum)
            </th bgcolor="c3c3c3">
            <th bgcolor="c3c3c3">
                @Html.DisplayNameFor(model => model.Procent)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Head)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Period)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Sum)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Procent)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.CreditId">Редактировать</a> |
                <a asp-action="Details" asp-route-id="@item.CreditId">Подробнее</a> |
                <a asp-action="Delete" asp-route-id="@item.CreditId">Удалить</a>
            </td>
        </tr>
}
    </tbody>
</table>
